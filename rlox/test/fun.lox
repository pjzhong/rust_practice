fun sayHi(first, last) {
    print "Hi, " + first + " " + last + "!";
}

sayHi("Dear", "Reader");

fun count(n) {
    if 0 < n {
        count(n - 1);
        print n;
    } else {
        print n;
    }
}

count(15);

print clock();